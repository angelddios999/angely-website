# Public (exposed to the browser via import.meta.env)
# Get this from Cloudflare Turnstile dashboard — safe to expose client-side
PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key-here

# Optional site metadata overrides for local preview
# PUBLIC_SITE_URL=http://localhost:4321
