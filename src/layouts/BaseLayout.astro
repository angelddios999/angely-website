---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { site } from "../data/site";

interface Props {
  title?: string;
  description?: string;
  /** Wider/narrower main content if needed */
  bare?: boolean;
}

const {
  title = site.title,
  description = site.description,
  bare = false,
} = Astro.props;

const pageTitle = title === site.title ? title : `${title} · ${site.shortName}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,500;0,9..144,600;0,9..144,700;1,9..144,400&display=swap"
      rel="stylesheet"
    />
    <title>{pageTitle}</title>
  </head>
  <body class="min-h-dvh flex flex-col">
    <a
      href="#main"
      class="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:rounded-lg focus:bg-ink focus:px-3 focus:py-2 focus:text-sm focus:text-canvas-elevated"
    >
      Skip to content
    </a>
    <Header />
    <main id="main" class:list={["flex-1", !bare && ""]}>
      <slot />
    </main>
    <Footer />
    <script>
      // Soft-protect portfolio media: block right-click save and drag-to-desktop.
      // This only raises the bar for casual copying — it is not true DRM.
      function isProtectedMedia(target: EventTarget | null): boolean {
        if (!(target instanceof Element)) return false;
        if (target instanceof HTMLImageElement || target instanceof HTMLVideoElement) {
          return (
            target.classList.contains("protect-media") ||
            !!target.closest(".protect-media, #lightbox-media, .card-media")
          );
        }
        return !!target.closest(".protect-media, #lightbox-media");
      }

      document.addEventListener(
        "contextmenu",
        (e) => {
          if (isProtectedMedia(e.target)) e.preventDefault();
        },
        { capture: true },
      );

      document.addEventListener(
        "dragstart",
        (e) => {
          if (isProtectedMedia(e.target)) e.preventDefault();
        },
        { capture: true },
      );
    </script>
  </body>
</html>
